Чтобы успешно защитить лабораторную работу по логистической регрессии, вам нужно уверенно ориентироваться в нескольких ключевых теоретических блоках: от основ алгоритма до тонкостей его оптимизации и оценки. Вот полный теоретический минимум, структурированный для подготовки к защите.

### 🎯 Основы Логистической Регрессии

1.  **Что это такое и когда применяется**
    *   Логистическая регрессия — это **алгоритм классификации**, а не регрессии, несмотря на название. Он используется для предсказания вероятности принадлежности объекта к определённому классу (например, выжил пассажир Титаника или нет) .
    *   Основная идея: найти **решающую границу** (гиперплоскость), которая наилучшим образом разделяет классы.

2.  **Гипотеза и Сигмоида**
    *   Гипотеза модели представляет собой **линейную комбинацию** признаков: `z = w_1*x_1 + w_2*x_2 + ... + w_n*x_n + b`, где `w` — веса, `b` — смещение (bias).
    *   Чтобы превратить линейный выход `z` в вероятность, используется **сигмоида (логистическая функция)**: `σ(z) = 1 / (1 + e^{-z})` .
    *   Эта функция "сжимает" значение `z` в интервал между 0 и 1, которую можно интерпретировать как вероятность `P(y=1 | x)`.

3.  **Функция потерь (Log Loss)**
    *   Для обучения модели нельзя использовать MSE, как в линейной регрессии, так как задача нелинейна. Вместо этого используется **логарифмическая потеря (Log Loss)** или **перекрёстная энтропия (Cross-Entropy)** .
    *   **Формула для бинарной классификации**:
        `J(w) = - (1/m) * Σ [y_i * log(σ(z_i)) + (1 - y_i) * log(1 - σ(z_i))]`
        где `m` — количество примеров, `y_i` — истинная метка (0 или 1), `σ(z_i)` — предсказанная вероятность.
    *   **Смысл**: функция сильно "штрафует" модель за уверенные, но неправильные предсказания.

### ⚙️ Методы Оптимизации (Как минимизировать потери)

Цель обучения — найти такие параметры `w` и `b`, при которых значение функции потерь `J(w)` минимально. Для этого используются алгоритмы, основанные на **градиентном спуске**.

1.  **Принцип градиентного спуска**
    *   На каждом шаге алгоритм вычисляет **градиент** функции потерь (вектор частных производных). Градиент указывает направление наискорейшего роста функции. Чтобы минимизировать её, мы делаем шаг в **противоположном** направлении .
    *   **Формула обновления весов**: `w = w - α * ∇J(w)`, где `α` — **скорость обучения** (learning rate).

2.  **Варианты градиентного спуска** 
    *   **Stochastic Gradient Descent (SGD)**: Параметры обновляются после расчёта градиента на **одном случайном** примере. Это быстро, но путь к минимуму очень шумный.
    *   **Mini-batch Gradient Descent**: Золотая середина. Градиент считается на небольшой группе примеров (batch). Это сочетает эффективность и стабильность.
    *   **Batch Gradient Descent**: Градиент вычисляется для **всего** набора данных. Обеспечивает стабильное движение к минимуму, но очень медленное и требовательное к памяти.

3.  **Продвинутые (адаптивные) методы оптимизации**
    Чтобы ускорить сходимость и избежать проблем стандартного SGD, были придуманы адаптивные алгоритмы, которые подстраивают скорость обучения для каждого параметра. В вашей лабораторной работе вы исследовали некоторые из них.

В таблице ниже сравниваются ключевые оптимизаторы, которые вам стоит знать:

| Алгоритм | Ключевая идея | Преимущества | Недостатки |
| :--- | :--- | :--- | :--- |
| **Momentum**  | Добавляет инерцию (`γ`), сглаживая градиент. | Ускоряет движение в нужном направлении, подавляет колебания. | Может проскочить минимум. |
| **RMSprop**  | Адаптирует LR на основе **среднего квадратов градиентов**. | Решает проблему затухания LR, хорошо работает для неквадратичных поверхностей. | Имеет собственный гиперпараметр (затухание). |
| **Adam**  | Комбинирует идеи **Momentum** (первый момент) и **RMSprop** (второй момент). | Эффективен, требует меньше настройки LR, популярен по умолчанию. | Больше гиперпараметров (`β1`, `β2`), требует больше памяти . |

**Метод Ньютона**, который вы также реализовывали, относится к **методам второго порядка**. Он использует не только градиент, но и **матрицу Гессе** (вторые производные), что позволяет учитывать "кривизну" функции потерь и сходиться к минимуму гораздо быстрее. Однако он требует больших вычислительных ресурсов и может быть неустойчив .

### 📊 Оценка качества модели

После обучения модель нужно оценить на тестовой выборке. Для бинарной классификации используются следующие метрики :

*   **Accuracy (Точность)**: Доля правильных ответов. `(TP + TN) / (Все примеры)`. Хороша для сбалансированных выборок.
*   **Precision (Точность)**: Доля реально положительных примеров среди тех, которые модель назвала положительными. `TP / (TP + FP)`. Показывает, насколько мы можем доверять, если модель сказала "да".
*   **Recall (Полнота)**: Доль положительных примеров, которые модель смогла обнаружить. `TP / (TP + FN)`. Показывает, насколько модель "чувствительна".
*   **F1-Score (F-мера)**: Среднее гармоническое между Precision и Recall. `2 * (Precision * Recall) / (Precision + Recall)`. Универсальная метрика, особенно важна для несбалансированных данных.

### 💡 Ключевые выводы для защиты

1.  **Суть алгоритма**: Чётко объясните, что логистическая регрессия — это линейный классификатор, который предсказывает **вероятность** с помощью **сигмоиды**.
2.  **Процесс обучения**: Подчеркните, что обучение — это процесс **минимизации log-loss** с помощью **градиентного спуска**.
3.  **Сравнение оптимизаторов**: Будьте готовы рассказать, чем **Adam** и **RMSprop** лучше базового градиентного спуска (адаптивный learning rate) и почему **метод Ньютона** может быть эффективным, но дорогим.
4.  **Анализ результатов**: Интерпретируйте метрики не по отдельности, а вместе. Объясните, почему, например, высокий Recall может быть важнее высокого Precision в задаче диагностики заболеваний. Проанализируйте **матрицу ошибок**.

Надеюсь, этот структурированный теоретический обзор поможет вам блестяще защитить лабораторную работу! Если какой-то из моментов требует более глубокого разъяснения, не стесняйтесь спрашивать.